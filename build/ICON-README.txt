App icon — Windows
==================

1. Prepare a square PNG (256x256 or 512x512 recommended).
2. Save it as:  build/icon.png
3. Run:  npm run electron:build

Optional: convert to .ico and save as build/icon.ico (used if present).

Tip: restaurant logo on solid background works best at small sizes.
